name: Rollback on 'prod'

on:
  workflow_dispatch:

jobs:
  deploy:
    name: Rollback on 'prod'
    uses: ./.github/workflows/deploy.yml
    with:
      environment: 'prod'
      prerequisite-environment: 'prod'
